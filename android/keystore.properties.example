# Copy to keystore.properties (gitignored). Never commit the real file.
# Path is relative to the android/ folder (project root).
storeFile=release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=moodlife
keyPassword=YOUR_KEY_PASSWORD
